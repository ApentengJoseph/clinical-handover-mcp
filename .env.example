# Clinical Handover MCP Server — Environment Variables
# Copy this file to .env and fill in values as needed.
# The server currently requires no secrets for local stdio use.

# Optional: set NODE_ENV for future environment-specific behaviour
NODE_ENV=production

# Future Notion integration (not yet used)
# NOTION_TOKEN=secret_xxxxx
# NOTION_WARD_PREFS_DB_ID=xxxxx
